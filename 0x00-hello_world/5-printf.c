#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void); /* Program should compile without warnings */

int main(void)
{
	/* Adding text with wall */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
